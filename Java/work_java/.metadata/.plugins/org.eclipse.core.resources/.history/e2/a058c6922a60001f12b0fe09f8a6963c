
// 예제 : 정수형 상수 저장하는 변수 메모리 만들기

public class Data03 {

	public static void main(String[] args) {

		// a = 1;   ---> error - 선언된 a 변수 메모리가 없기 때문에 1을 저장할 수 없다
		
		// 자바에서는 변수 메모리에 저장된 데이터를 꺼내서 사용하기 위해서는
		// 먼저 변수 메모리를 생성(선언) 해야 한다.
		
		// 변수 메모리 생성(선언) 문법
		// 자료형 변수이름;
		
		// 저장될 값의 종류에 따라 자료형을 결정해야 하는데,
		// 만일 1과 같은 정수형 상수를 저장하기 위해서는
		// 다음과 같이 정수형 변수를 선언하면 된다.
		
		// 4 바이트 크기의 변수 메모리에 정수를 저장하기 위해서는
		// int 자료형으로 a 라는 이름의 변수 메모리 생성(a 변수 선언)
		
		int a; // a 변수 메모리 [] 선언하고,
		
		a = 1; // a 변수 메모리 []에 1 정수형 상수 값을 저장
			   // a 변수 메모리 [1]
		
		//// println 구문으로 a 변수 메모리[1]에 저장된 1을 얻어 출력!
		System.out.println(a); 
		
		a = 2; // 변수 메모리에 다른 값을 대입하면 변수 메모리에 저장된 값은 제거 되고
		       // 다른 값을 저장하여 변수 메모리에 저장되는 값을 변경할 수 있다.
			   // 1. a 변수 메모리 [1]
			   //    a = 2;    -->  a 변수 메모리에 2 저장
			   // 2. a 변수 메모리 [2] 1은  제거되고 마지막에 저장시킨 2가 저장
		
		// 마지막에 대입한 값만 유지되는지 출력해보자
		System.out.println(a); // 2

	}

}
